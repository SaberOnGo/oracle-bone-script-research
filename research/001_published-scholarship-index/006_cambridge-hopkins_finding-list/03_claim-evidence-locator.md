# Claim-Evidence Locator / 说法与证据定位

## Official Page Locators / 官方页定位

| State | Claim | Exact locator |
| --- | --- | --- |
| source-reported | Page identity | HTML title and first heading |
| source-reported | Digitised bones and 50 in 3D | opening paragraph |
| source-reported | `609` grand total | `Classified Table of Contents` |
| source-reported | `c/h/j/y` meanings | `KEY` heading |
| source-reported | Per-row cross-references | `PERIOD I` to `PERIOD V` |
| source-reported | Four `ybu` routes | terminal `Unclassified` heading |

The page has no stable printed pagination. A reviewer must cite the official
URL, access date, heading, period, group, and full row key rather than invent
a page number.

该网页没有稳定印刷页码。复核者应记录官方 URL、访问日期、标题、时期、
组别和完整行键，不应虚构页码。

## Independently Checked Local Locators / 本地独立核查定位

- Download fact: `project_registry/006_large-source-register/`
  `002_source-download-log.csv`, row
  `dl-cambridge-hopkins-finding-list`.
- Section audit:
  `corpus/006_research-sources-and-bibliography/001_source-objects/`
  `008_src-cambridge-hopkins_source-object/`
  `21_finding-list-reconciliation.md`.
- Machine cross-check: the same source-object directory,
  `22_finding-list-reconciliation-index.json`.
- Row trail: the same directory,
  `23_finding-list-row-reconciliation.csv`.

These paths were `independently-checked` on `2026-08-13`. They prove what
the repository retained and calculated, not what each bone means.

这些路径于 `2026-08-13` 完成本地 `independently-checked`。它们只能证明
仓库保留和计算了什么，不能证明每件甲骨的含义。

## Unresolved Locators / 未解决定位

Every route below remains `unresolved`.

- Which exact CUL object page, image direction, and rights statement belongs
  to each `c` number?
- Which Chalfant page or plate supports each `h` number?
- Which *Heji* page, plate, rubbing, and text supports each `j` number?
- Which *Yingguo* volume, page, plate, and image supports each `y` number?
- Which row-layout rules explain the `609`/`612` and section differences?

- 每个 `c` 号对应哪一个 CUL 实物页、图像方向和权利说明？
- 每个 `h` 号对应 Chalfant 的哪一页或哪一图版？
- 每个 `j` 号对应《合集》的哪一页、图版、拓片和文本？
- 每个 `y` 号对应《英藏》的哪一卷、页、图版和图像？
- 哪些页面行结构可以解释 `609`/`612` 及分节差异？
