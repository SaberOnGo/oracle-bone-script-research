# Visual region review / 图像区域复核

## Image-first record / 图像优先记录

The two committed images are direct, unchanged API downloads:

- `03_visual-assets/001_asset-000001_met-42045-image-002.jpg` is the API
  `primaryImage` view.
- `03_visual-assets/002_asset-000002_met-42045-image-001.jpg` is the API
  `additionalImages[0]` view.

Both images show the same narrow, elongated bone-like object against a light
background, but the API does not define their orientation relationship. The
primary view contains denser visible incised marks. The additional view shows
the red accession marking and a less densely marked surface.

两张图像都是 API 直接获取且未改动的文件。两张图像都显示浅色背景上的细长
骨状对象，但 API 没有定义两者的方向关系。primaryImage 的可见刻痕更密，
additionalImages[0] 可见红色馆藏号标记和较少刻痕的表面。

## Region status / 区域状态

No box, sign count, line order, or OCR region is registered. A future region
register must bind every box to an image SHA-256, pixel coordinate system,
orientation note, and an explicit uncertainty state.

当前没有登记框、字数、行序或 OCR 区域。未来区域表必须把每个框绑定到图像
SHA-256、像素坐标系、方向说明和明确的不确定状态。

This page is a visual observation record, not a transcription or reading.

本页是视觉观察记录，不是摹写或释读。

The earlier high-contrast photograph now provides a third observation route.
It is reviewed in `11_historical-inscribed-face-evidence.md`. It does not by
itself authorize sign boxes or a text order.

早期高反差照片现在提供第三条观察路线，详见
`11_historical-inscribed-face-evidence.md`。它本身仍不足以授权逐字框或
文字次序。
