# AI Diagnostic Pilot v4 / AI 诊断试点 v4

Status / 状态: reviewed

research_boundary: benchmark_pilot_not_scholarship

delivery_status: withheld

## Human opening / 人类入口

This is a human-readable audit of one negative-control pipeline run. It is
not a decipherment result, a research probability, or a scholarly conclusion.

这是一次负对照流程运行的人类可读审计。它不是释读结果、研究概率，
也不是学术结论。

The source ancestor is the HUST-OBC object route. The Agent view contained
only a coordinator-produced text observation. No source image was copied into
this record, and no restricted image is redistributed here.

来源祖先是 HUST-OBC 对象路线。Agent 看到的只有协调器生成的文字观察。
本记录没有复制来源图片，也没有再分发受限图片。

The material route remains `source_marked_risk_noted`. The diagnostic view is
metadata-only and must not be treated as a public image or a catalog identity.

资料路线仍为 `source_marked_risk_noted`。诊断视图仅限元数据，不能当作
公开图片或著录身份。

## What was frozen / 冻结内容

The case was a `null_or_negative_control` with three opaque candidates,
including `unknown_or_other`. The evidence cutoff was recorded before the
run openings. The frozen-input SHA-256 was:

案件类型为 `null_or_negative_control`，候选集含三个不透明 ID，
包括 `unknown_or_other`。证据截止时间早于两次开跑回执。冻结输入
SHA-256 为：

`64bc73f5e7526c68a510827502d0cf65df96a5e50747a06ddd8a31d13451171a`

The candidate-manifest SHA-256 was:

候选清单 SHA-256 为：

`152ec01ca7944bb51877c11302e11524bf93e8f504191f9d35a716a9c0e97ab6`

The locked prompt-manifest SHA-256 was:

锁定提示清单 SHA-256 为：

`25a3942b3aaa695a4bc67af5f2ac5387f2d01319d378f3090c0d0544075b9bbb`

The public commitment SHA-256 was:

公开承诺文件 SHA-256 为：

`6b2974e4e5f0f4dcb589b1b0fafa529392e4a3bf9f2636b478c6fd90924e4025`

## Run chain / 运行链

Two run openings were created before dispatch. Both used the same recorded
model family and were fresh execution contexts. This was a same-model
execution rerun, not model-independent.

Run type: same-model execution rerun.
Independence: not model-independent.

两次运行均在派发前创建开跑回执。两次使用同一记录的模型族，且上下文
分别新建。第二次是执行复跑，不是模型独立复跑。

The primary locked-run SHA-256 was:

主运行锁定回执 SHA-256 为：

`1091511a80232989bc2e6e611fe4de23db25025c6da0db586aea62fab6fe7505`

The execution-rerun locked-run SHA-256 was:

执行复跑锁定回执 SHA-256 为：

`55aa9301e1a503352ffda2c52beae9ac5f4c2bd8b23df3a4d0fddbbdbdd5225f`

The two raw Agent output hashes were recorded in their locked reports as
`f01b180d64bcf8f9791f16383639fed489816e7f6f8482c5ff086af07a07a2b1` and
`8d6dc2d4fa9252f2dce322ff4d72beac7be08accfa2212fcbf29fc743d05aac2`.

两份原始 Agent 输出的哈希已写入锁定报告，分别为上述两值。原始输出、
工作草稿和私有密封材料均留在忽略区，不进入本档案。

## Observed result / 观察结果

Both runs abstained and selected no candidate. The primary run ranked an
opaque A ID first; the execution rerun ranked `unknown_or_other` first. Both
recorded leakage as indeterminate and retained
`pretraining_exposure_unknown`.

两次运行都弃权且没有选择候选。主运行把一个不透明 A ID 排在首位；
执行复跑把 `unknown_or_other` 排在首位。两次都将泄漏标为
`indeterminate`，并保留 `pretraining_exposure_unknown`。

The one-shot local score therefore recorded:
`diagnostic_fail_withheld`.

因此，一次性本地评分记录为：`diagnostic_fail_withheld`。

This disagreement is retained as counterevidence. It is not averaged into a
probability and does not enter a candidate-delivery channel.

这项不一致被保留为反证。它没有被平均成概率，也没有进入候选交付通道。

The local score receipt SHA-256 was:

本地评分回执 SHA-256 为：

`731D46A2872DCD45803D7603525CB277D717C7580E9F8DDCFFCDD7416E5EB33F`

The private label was opened once for this diagnostic and then retired. Its
meaning was only the closed negative-control label for this frozen view; it
does not assert a permanent claim about the object.

本次诊断只开封一次私有标签，随后立即退役。该标签只表示这个冻结视图
中的封闭负对照标签，不对对象本身作永久判断。

## Limits and discarded attempts / 限制与废弃尝试

The local commitment has no external signature or independent timestamp.
Procedural isolation was recorded, but no cryptographic process sandbox was
available. Model independence, absence of pretraining exposure, and
probability calibration remain unproven.

本地承诺没有外部签名或独立时间戳。记录了程序性隔离，但没有密码学进程
沙箱。模型独立性、没有预训练暴露以及概率校准仍未被证明。

Earlier v1, v2, and v3 artifacts were retained as invalid or discarded
diagnostics because their bindings or evidence routes were insufficient. They
were not repaired, scored, or selected for reporting.

此前 v1、v2、v3 产物因绑定或证据路线不足而保留为无效或废弃诊断；
它们没有被修补、评分或择优报告。

This record is not Gate 3, not a calibrated benchmark, not an AI candidate,
and not a decipherment result. Human specialists remain optional escalation
and audit routes, while this run itself must remain withheld.

本记录不是 Gate 3，不是校准基准，不是 AI 候选，也不是释读结果。
真人专家仍可作为可选升级和审计路线；本次运行必须保持扣留。
