# Claim Evidence Gate Review / 命题证据门槛复核

## English

This page applies the claim matrix to IHP item `503`, accession `R044498`,
on 2026-08-21. It records source and preprocessing status only. It does not
create a transcription, translation, character assignment, or reading.

### Evidence states

- `E1` is the IHP English and Chinese object-page route. It is
  `source_reported` for the title, period, YH127 findspot, and `帝令雨`
  description.
- `E2` is the ignored high-definition image snapshot recorded by the parent
  collection-object dossier. It is directly checked locally, but its rights
  remain `metadata_only_until_verified`.
- `E3` is the parent dossier's pixel observation. It is not a plate, OCR, or
  transcription and does not prove that separated marks form one sentence.

### Claim dispositions

- `C1 object identity`: `blocked`. Accession and museum fields are stable
  routes, but physical history, publication identity, and rights for a public
  derivative remain unresolved.
- `C2 direct glyph observation`: `direct_checked` for the private snapshot.
  The visual page records fragments, gaps, breaks, colour, measurement marks,
  and possible later annotations without assigning their identity.
- `C3 sign, variant, near-form, or component`: `blocked`. No region, alignment,
  comparison family, or non-label character support is fixed.
- `C4 inscription occurrence and context`: `blocked`. `帝令雨` is a source
  description with no line locator, full text, OCR checksum, plate, or edition.
- `C5 reading or phonological candidate`: `blocked` by C4 and missing reading
  history, proposer, alternatives, and positive/negative text evidence.
- `C6 semantic or grammatical function`: `blocked`. The page topic cannot be
  treated as a project translation or sentence-level grammatical evidence.
- `C7 diachronic correspondence`: `blocked`. No period-provenanced comparison
  forms or bridge argument is attached.
- `C8 complete proposition and user delivery`: `withheld`. Rights, calibration,
  independent rerun, external scoring, and C1--C7 blockers remain open.

### Concrete next-source questions

1. Which publication or catalog gives a plate and line locator for `R044498`?
2. Which permitted rubbing or facsimile distinguishes marks from cracks,
   colour bars, measurements, and later annotations?
3. Which independent text edition records `帝令雨`, its neighbours, and a
   full sentence without treating the museum prose as OCR?
4. Which dated scholars proposed readings, and where are contrary readings?
5. Which rights statement permits a public crop or text derivative?

No decipherment probability is estimated. The candidate remains
`source_record_candidate_needs_catalog_and_text_review`, with no user-facing
candidate delivery.

## 简体中文

本页把命题矩阵应用于史语所 503 号对象、馆藏号 `R044498`，日期为
2026-08-21。它只记录来源和预处理状态，不生成摹写、译文、单字关联或释读。

### 证据状态

- `E1` 是史语所中英文对象页路线。对题名、时期、YH127 出土地和
  `帝令雨` 说明，它属于 `source_reported`。
- `E2` 是父级馆藏对象档案登记的被忽略高清图像快照。本地已直接检查，
  但有效权利仍为 `metadata_only_until_verified`。
- `E3` 是父级档案中的像素观察，不是图版、OCR 或摹写，也不能证明分离
  痕迹属于同一句卜辞。

### 命题处置

- `C1 对象身份`：`blocked`。馆藏号和馆方字段路线稳定，但实物史、出版
  身份以及公开派生物权利仍未解决。
- `C2 直接字形观察`：私有快照为 `direct_checked`。视觉页记录残片、空隙、
  断口、颜色、测量标尺和可能的后加标记，不分配其身份。
- `C3 同字、异体、近形或构件`：`blocked`。没有字区、对齐、比较家族或
  非标签单字证据。
- `C4 卜辞字例与上下文`：`blocked`。`帝令雨` 是来源说明，没有行定位、
  全文、OCR checksum、图版或版本。
- `C5 读音或隶定候选`：因 C4 以及释读史、提出者、替代读法和文本正反证
  缺失，`blocked`。
- `C6 语义或语法功能`：`blocked`。页面主题不能当作项目译文或句法证据。
- `C7 历时字形对应`：`blocked`。没有带时期来源的比较字形或桥梁论证。
- `C8 完整命题与用户交付`：`withheld`。权利、校准、独立复跑、外部评分和
  C1--C7 阻断项仍未完成。

### 具体下一来源问题

1. 哪一份出版物或著录能给出 `R044498` 的图版和行定位？
2. 哪一份获准拓片或摹本能区分刻痕、裂纹、色条、测量标记和后加标记？
3. 哪一份独立文字版本能记录 `帝令雨`、邻字和完整句子，而不是把馆方说明
   当作 OCR？
4. 哪些有日期的学者提出过释读，相反释读在哪里？
5. 哪条权利说明允许公开裁片或文字派生物？

没有估计任何破译概率。本候选仍为
`source_record_candidate_needs_catalog_and_text_review`，没有面向用户的候选
交付。
