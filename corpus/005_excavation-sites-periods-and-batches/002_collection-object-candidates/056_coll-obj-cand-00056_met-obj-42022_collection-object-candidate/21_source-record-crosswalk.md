# Source-record crosswalk / 来源记录交接

Project object: `coll-obj-cand-00056` / Met object `42022`.

This page joins the collection-object dossier to the source-record candidate
for the same museum object. It does not create a second identity, text, or
reading.

本页把馆藏对象档案与同一博物馆对象的来源记录候选连接起来。
它不新建第二个身份、文字或释读。

## Matched routes / 对应路线

- Collection object: `42022`, accession `18.56.71`.
- Source-record candidate: `obs-insc-src-cand-000009`.
- Source-record area:
  [`002_oracle-bone-inscriptions`](../../../002_oracle-bone-inscriptions/)
- Candidate folder: `009_obs-insc-src-cand-000009_met-42022_`
  `source-record-candidate`.
- Read `README.md`, `01_object-and-image-routes.md`, and
  `09_two-view-human-evidence.md` inside that candidate folder.

The two routes share the museum object ID, accession number, API URL, and API
checksum. The source-record candidate is the local entrance for both image
bytes; this collection-object folder keeps one compact asset as its visual
entrance.

两条路线共享博物馆对象 ID、馆藏号、API URL 和 API checksum。
来源记录候选是两张图像字节的本地入口；本馆藏对象目录保留一张紧凑资产
作为图像入口。

## Two API image routes / 两条 API 图像路线

- `primaryImage` -> `001_asset-021414_met-42022-image-002.jpg`;
  2508142 bytes; SHA-256
  `61510f04...adad8cd`.
- `additionalImages[0]` -> `002_asset-021415_met-42022-image-001.jpg`;
  2643473 bytes; SHA-256
  `c58ede9b...690122e`.

The API does not define a recto-verso relationship. View A has more visible
linear marks, while View B shows more porous bone and a red accession marking.
This is a visual contrast only, not a text, identity, or reading alignment.

API 没有定义正反面关系。视图 A 的线性痕迹更明显，视图 B 更明显显示多孔
骨面和红色馆藏标记。这只是视觉差异，不是文字、身份或释读对应。

## Still blocked / 仍然阻断的事项

- No Heji number, plate, page, findspot, excavation unit, or full text is
  supplied by the API snapshot.
- No source establishes a stable orientation or line-addressable sign
  segmentation across the two views.
- No project inscription ID, character ID, component assignment, reading,
  translation, or decipherment conclusion is assigned.
- Before formal comparison, locate an accession-linked catalog or publication
  plate and bind every proposed text unit to that external evidence.

- API 快照没有提供合集号、图版、页码、出土地、发掘单位或全文。
- 没有来源建立两图之间稳定的方向或可定位字形切分。
- 项目没有分配卜辞 ID、单字 ID、构件归属、释读、翻译或破译结论。
- 正式比较前，必须找到馆藏号关联的著录或出版图版，并把每个拟议文字
  单位绑定到外部证据。

Review status / 复核状态:
`source_record_candidate_needs_text_and_catalog_review`.

