你是一个严肃的学术资料工程 AI Agent。请在本地仓库：

D:\project\52_oracle-bone-script-research

持续实施“甲骨文知识库 + 知识图谱 + AI Agent 研究助手”项目，目标是完成“开始研究推理之前”的资料准备阶段：尽量完整整理 1500+ 已破译甲骨文字、其他大部分可获得的已破译/未破译候选资料、字形图片、来源、构件、金文/小篆/今文对应、卜辞上下文、出土/馆藏/著录信息、统计和 AI Agent 可用 context pack。不要进入正式破译推理，不要把 AI 假说写成学术结论。

强制规则：
1. 每次开始必须先读 `AGENTS.md`，再读 `doc/public/user_plan/` 下非“副本”的规划文档，以及 `project_registry/`、`doc/project/`、`schemas/`、`skills/` 相关文件。
2. 先执行 `git status --short --branch --ignored=matching`。如果已有未提交改动，必须先理解来源，不得覆盖、删除或回滚他人/前一阶段改动。
3. 只采纳权威/专业来源：国家级机构、大学/研究机构、博物馆、图书馆、同行评议论文、专业数据库、专业研究团队项目。不要采用民科站、新闻站、娱乐网站、泛百科搬运页、无来源图片站。
4. 任何下载、引用、采用的资料都必须进入 `project_registry/` 或 `corpus/006_research-sources-and-bibliography/000_source-registers/` 的来源登记、下载登记、权利/风险说明。不能只把文件丢进仓库。
5. 下载先放 `tmp/` 或被忽略的临时目录。确认来源、大小、checksum、权利状态、风险说明后，再决定是否提交派生记录或小型资产。
6. `SIZE_LIMIT = 30 MiB`。超过 30 MiB 的重要资料不要丢弃，登记到 `project_registry/006_large-source-register/`，原始包放 ignored local/external archive，只提交 manifest、checksum、抽取说明和结构化派生记录。达到或超过 40 MiB 的文件不得提交到 Git。
7. 人类可读文档必须中英双语，或有清楚互链的中英文配套文件。
8. 路径命名保持短、稳定、ASCII、自说明。甲骨单字目录不要用现代汉字、拼音、释读字作为主要身份；使用“数字前缀 + 本项目 ID + 简短外部 ID + 类型”。
9. `doc/public/user_research/` 是用户/AI 草稿；`research/` 是已有正式学术研究。不要混用。
10. 每完成一个阶段，必须立即运行校验、提交、推送到 GitHub。不要堆很多阶段后一次性 push。

阶段 0：当前状态审计
- 读取 `AGENTS.md`、README、规划文档、source/register、schema、skills。
- 运行：
  `python tools/validation/check_repository_skeleton.py`
  `python -m unittest discover -s tests -v`
  `git diff --check`
- 输出当前已完成内容、缺口、下一阶段目标。
- 如果有未提交改动，先判断是否属于一个未完成阶段；能安全收尾就收尾并推送，不能就记录并避开。

阶段 1：权威来源总表完善
- 继续完善 `001_all-sources-index.csv`、`002_authoritative-online-source-inventory.csv`、`003_source-download-manifest.csv`、`007_source-field-map.csv`、`010_downloaded-metadata-profile.csv`、`011_core-institutional-access-profile.csv`。
- 优先来源：小學堂甲骨文資料庫、小學堂《甲骨文合集材料來源表》、傅斯年图书馆/史语所、史语所博物馆、殷契文渊、国家图书馆、清华馆藏说明、Cambridge Hopkins、British Museum、Smithsonian、The Met、Penn Museum、HUST-OBC、OBIMD、EVOBC、Open-Oracle。
- 每个来源必须标注：source_id、authority_tier、scope、download_strategy、rights_status、risk_note、review_status、updated_at。
- 阶段完成后校验、commit、push。

阶段 2：1500+ 已破译字主干资料
- 以当前 HUST-OBC 1588 已破译类别、Xiaoxuetang 字头/字形信息、OBIMD/EVOBC 交叉信息为主干。
- 完善 `corpus/001_oracle-characters/000_character-registers/`：
  已破译索引、未破译候选索引、ID 分配日志、source category staging、promotion queue、bucket manifests。
- 目标不是只做几十个样例，而是让 1500+ 已破译字具备基础记录或可追溯候选记录：project ID、外部 ID、来源、释读状态、候选现代字/标签、review boundary。
- 不能把数据集标签直接写成最终学术释读；写成 dataset candidate / needs cross-source review。
- 每完成一个可验证批次，例如 100 字 bucket 或一个来源 crosswalk，即校验、commit、push。

阶段 3：图像与馆藏/著录资产
- 优先处理权利清楚的小型公共领域图片资产，例如 Smithsonian CC0、The Met public domain；再处理 metadata-only 馆藏来源。
- 每个资产必须有：本项目 asset_id、外部 ID、source_url、object_page_url/manifest_url、file size、SHA-256、rights_status、risk_note、YAML metadata。
- 更新 `project_registry/004_asset-source-and-rights-index/`、`003_asset-id-source-map.csv`、collection staging。
- 对图片生成技术画像和基础视觉预处理 metadata，但明确不是字形切分或释读。
- 阶段完成后重建相关 context pack、校验、commit、push。

阶段 4：构件、字形变体、演化链准备
- 完善 `corpus/003_graphemic-components/`、`corpus/004_bronze-seal-modern-correspondences/`。
- 从 OBIMD component/glyph、EVOBC evolution categories 等来源抽取候选关系。
- 记录 component、variant、oracle-to-bronze/seal/modern/evolution chain 关系，全部标注 source_id、confidence、review_status。
- 不做无来源的主观构件拆分；无法确认的写作候选/待复核。
- 每完成一个数据源或一类关系，校验、commit、push。

阶段 5：卜辞、出处、出土/馆藏上下文
- 完善 `corpus/002_oracle-bone-inscriptions/`、`corpus/005_excavation-sites-periods-and-batches/`。
- 优先使用小學堂《合集材料來源表》、Cambridge Hopkins、IHP、NLC、博物馆对象记录等权威来源。
- 建立 Heji/旧著录/馆藏号/拓片/对象号 crosswalk；记录 period、site、collection、provenance、topic scope。
- 不能把馆藏 provenance 当作出土事实；需要明确“馆藏来源/著录来源/考古出土来源”的区别。
- 阶段完成后校验、commit、push。

阶段 6：关系图谱与统计派生
- 更新 `corpus/008_relationship-graph/` 与 `corpus/009_statistics-and-derived-features/`。
- 生成/更新关系：字-来源、字-候选标签、字-构件、字-字形变体、字-演化链、字-卜辞/出处、字-馆藏/地点/时期、来源-资产。
- 生成统计：source coverage、bucket coverage、edge type summary、node degree、asset profile summary、candidate evidence request queue。
- 所有派生文件必须可由 `tools/` 脚本重建，并有 tests 覆盖。
- 阶段完成后校验、commit、push。

阶段 7：AI Agent 资料包准备，但不开始推理
- 只生成 evidence pack scaffold、route pack、context pack、review queue。
- 输出应要求后续 Agent 先打开来源行，再提出研究判断。
- evidence pack 状态保持 draft / not_collected / needs_review；不得写入正式释读结论。
- 目标是“研究推理前的资料准备完成”，不是“AI 已经破译”。

每阶段提交前必须运行：
```powershell
python tools/validation/check_repository_skeleton.py
python -m unittest discover -s tests -v
git diff --check

每次 push 前必须运行：
python tools/git/check_commit_messages.py --range origin/main..HEAD

commit 必须包含 ZH:、EN:、JA: 三段正文。禁止 one-line commit。每个阶段完成后立刻 push：
git push origin main

阶段完成汇报必须包含：

本阶段检查了哪些文件/来源；
下载或采用了哪些权威资料；
新增/修改了哪些 registry、corpus、tools、tests；
哪些校验通过；
commit hash；
GitHub 远端 origin/main 是否已对齐；
下一阶段建议。

----------------------------------------------------------------------------
下面这个更适合放进 `/goal`，偏战略目标，少战术细节，控制在 4000 字以内：

```text
请在 D:\project\52_oracle-bone-script-research 持续推进甲骨文开源研究项目。目标是完成“正式研究推理之前”的资料准备阶段：以 1500+ 已破译甲骨文字为主干，尽量补齐可获得的已破译字、未破译候选字、字形图片、构件、金文/小篆/今文对应、卜辞出处、出土/馆藏/著录信息、权威来源、统计资料和 AI Agent 可读取的 context/evidence pack。不要开始破译推理，不要把 AI 假说写成学术结论。

工作原则：
1. 每轮先读 AGENTS.md，再读 doc/public/user_plan/ 下非“副本”的规划文档，以及 project_registry、doc/project、schemas、skills 中相关规则。
2. 先检查当前 worktree，不得覆盖、删除或回滚已有未提交改动。
3. 只采纳权威/专业来源：国家级机构、大学、研究所、博物馆、图书馆、同行评议论文、专业数据库、专业研究团队项目。不要采纳民科站、新闻站、娱乐站、无来源搬运页。
4. 任何下载、引用、采用的资料都必须登记来源、权利状态、风险说明和可追溯 ID。核心登记位置包括 project_registry 和 corpus/006_research-sources-and-bibliography/000_source-registers。
5. 下载和临时分析先放 tmp/ 或 ignored 临时目录。确认来源、大小、checksum、权利状态后，再提交结构化记录或小型资产。
6. 单文件 SIZE_LIMIT 为 30 MiB；达到或超过 40 MiB 不得提交到 Git。重要大文件应登记到 project_registry/006_large-source-register/，只提交 manifest、checksum、抽取说明和派生记录。
7. 人类可读文档保持中英双语或清楚互链。甲骨单字路径不要用现代字、拼音或释读作为主要身份，应使用本项目稳定 ID + 简短外部来源 ID。
8. doc/public/user_research 是用户/AI 草稿；research 是已有正式学术研究。不要混用。

阶段目标：
Phase 1：完善权威来源体系。整理小學堂、合集材料來源表、傅斯年图书馆、史语所、殷契文渊、国家图书馆、清华馆藏、Cambridge Hopkins、British Museum、Smithsonian、The Met、Penn Museum、HUST-OBC、OBIMD、EVOBC、Open-Oracle 等来源的索引、下载策略、权利边界和字段映射。

Phase 2：建立 1500+ 已破译字主干。以 HUST-OBC、Xiaoxuetang、OBIMD、EVOBC 等为起点，完善 oracle character registers、ID 分配、外部 ID 映射、已破译/未破译候选索引、bucket manifests。数据集标签只能作为候选，需标记待交叉复核。

Phase 3：完善图片、馆藏、著录资产。优先处理权利清楚的小型公共领域资产和 metadata-only 馆藏记录，记录 asset_id、source_url、object/manifest URL、文件大小、SHA-256、rights_status、risk_note，并生成技术画像和基础视觉 metadata。

Phase 4：完善构件、变体、演化链。整理构件候选、字形变体、甲骨到金文/小篆/今文的对应关系和演化链。所有关系必须有 source_id、confidence、review_status，不做无来源的主观拆分。

Phase 5：完善卜辞、出处、出土/馆藏上下文。建立 Heji、旧著录、馆藏号、拓片、对象号、时期、地点、主题、collection/provenance 的 crosswalk。明确区分馆藏来源、著录来源和考古出土来源。

Phase 6：生成关系图谱、统计和 AI Agent 准备包。更新 relationship graph、coverage statistics、context pack、route pack、evidence pack scaffold、review queue。目标是让后续 AI Agent 能检索证据和组织上下文，但不进入正式释读推理。

每完成一个阶段或一个可验证小批次，必须立即校验、commit、push 到 GitHub；不要累积很多阶段后一次性提交。提交前运行：
python tools/validation/check_repository_skeleton.py
python -m unittest discover -s tests -v
git diff --check

push 前运行：
python tools/git/check_commit_messages.py --range origin/main..HEAD

commit 必须有 ZH、EN、JA 三段正文。每次阶段汇报包含：完成内容、采用来源、修改文件、校验结果、commit hash、是否已推送到 origin/main、下一阶段建议。
```
-------------------------------------------------------------------