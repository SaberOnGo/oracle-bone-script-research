# Rubbing And Facsimile Routes / 拓片与摹本路线

## English

The H2 row points to two pixel-aligned source members. The rubbing member is
`rubbing/h00002.jpg` inside the ignored local package `rubbing.zip`. It is a
1022 by 1180 grayscale JPEG, 77,614 bytes, with member SHA-256
`1ae9e411f0356cb9dc232d629d4620b0e5f66f42c83300ce95775950a75b01e5`.
The package SHA-256 is
`4d07dca94e94c2d17edd7fa25be72b5673161c0c2d03dac4d2c094e5341b7747`.

The aligned facsimile member is `facsimile/h00002.jpg` inside
`facsimile.zip`. It is a 1022 by 1180 RGB JPEG, 49,395 bytes, with member
SHA-256
`ebc8aa5046dbb74e08e7dd0b74ff1ce9e24693dbe8dad91194bc438024340995`.
The package SHA-256 is
`b1544e34ee1a6a34fc0a83475a227fd2141a67293f795eaa3c52760fedb50b0e`.

Both packages remain under
`external_local_archive/source_packages/obimd/`. Their public visibility is
`local_private_route_only`. A reviewer may open the members locally for
comparison, but must not extract or commit them through this dossier.

## 简体中文

H2 来源行指向两个像素对齐的来源包成员。拓片成员为已忽略本地来源包
`rubbing.zip` 内的 `rubbing/h00002.jpg`。它是 1022×1180 灰度 JPEG，
大小 77,614 字节；成员和来源包的 SHA-256 已在上文及辅助 JSON 中固定。

对齐摹本成员为 `facsimile.zip` 内的 `facsimile/h00002.jpg`。它是
1022×1180 RGB JPEG，大小 49,395 字节；成员和来源包的 SHA-256 同样
已经固定。

两个来源包继续保存在
`external_local_archive/source_packages/obimd/`。公开可见性为
`local_private_route_only`。复核者可在本地打开成员进行并排比较，但不得
通过本档案解出并提交图片。

## Direct Visual Check / 直接图像复核

On `2026-08-12`, the primary Agent opened both members from the ignored
archives and compared them side by side. Both show the same irregular bone
outline and matching sign positions: three signs descend near the centre,
while another group lies along the right edge. The rubbing contains heavy
surface texture and uneven dark areas; the facsimile replaces that texture
with clean outlines and strokes. This confirms that the two routes are
visually corresponding presentations of the H2 source row. It does not
confirm any transcription, sign identity, or catalog identity.

`2026-08-12`，主 Agent 从已忽略归档中打开两个成员并排比较。
两图显示同一不规则骨片轮廓和相应字位：中部附近有三处纵向
字形，右缘另有一组。拓片有明显表面纹理和深浅不均的黑区；
摹本则以清晰轮廓和线条代替这些纹理。这足以确认两条路线是
H2 来源行的对应视觉呈现，但不确认释文、字形身份或著录身份。

## Visibility Boundary / 可见性边界

The effective rights status is `metadata_only_until_verified`. This page
therefore publishes only member locators, dimensions, sizes, and integrity
evidence. It does not publish either visual payload and does not claim that
the facsimile is a photograph, original artifact, or independent plate.

当前有效权利状态是 `metadata_only_until_verified`。因此本页只公开成员
定位、尺寸、大小和完整性证据，不公开图片内容，也不把摹本说成照片、
实物或独立图版。
