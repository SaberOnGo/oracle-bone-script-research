# Claim Evidence Gate Review / 命题证据门槛复核

## English

This page applies the repository claim-evidence matrix to
`obs-unk-000002` on 2026-08-21. It is a human research control record,
not a paleographic conclusion or a decipherment result.

### Evidence families

- `E1` is the committed JPEG derivative and its YAML metadata. The checksum,
  size, and pixels are directly checked.
- `E2` is the ignored HUST package member audit in
  `15_source-member-evidence-review.md`. It is the same HUST source family,
  not an independent witness.
- `E3` is the existing visual observation in
  `14_material-visual-observation.md`. It records visible marks only.

### Claim dispositions

- `C1 object identity`: `blocked`. The dataset route has no bone, fragment,
  edition, plate, collection, or stable catalog identity.
- `C2 direct glyph observation`: `direct_checked` for E1--E3. The record is
  limited to visible marks, dimensions, and source binding.
- `C3 same-sign, variant, near-form, or component`: `route_only`. No second
  form, independent family, or label-independent comparison is available.
- `C4 inscription occurrence and context`: `blocked`. No plate, line, exact
  position, neighbouring signs, OCR, or full text is linked.
- `C5 reading or phonological candidate`: `withheld` by C1 and C4 blockers,
  and by missing dated reading history and alternatives.
- `C6 semantic or grammatical function`: `withheld`. There is no sentence
  context or independently established sign identity.
- `C7 diachronic correspondence`: `withheld`. No period-provenanced bridge
  or independent later-form route is present.
- `C8 complete proposition and user delivery`: `withheld`. Rights, context,
  independent evidence, calibration, and C1--C7 blockers remain unresolved.

### AI decision

The current decision is `abstain_withhold_candidate`. No reading, meaning,
probability, or user-facing candidate is released. The object remains
`unknown_candidate_id_not_formal_obs_char_assignment`.

### Falsification conditions

- Withdraw any identity route if an authorized plate maps the image to a
  different object or if the filename is shown to be a generic placeholder.
- Withdraw any visual grouping claim if a second view shows that the apparent
  marks arise from damage, cropping, tracing, or neighbouring traces.
- Reopen only after a primary object route, a second evidence family, and a
  dated reading-history route are independently recorded.

## 简体中文

本页于 2026-08-21 将仓库命题—证据矩阵应用到 `obs-unk-000002`。
它是人类研究控制记录，不是文字学结论或破译结果。

### 证据家族

- `E1` 是已提交 JPEG 派生图及 YAML metadata，已直接核对 checksum、大小
  和像素。
- `E2` 是 `15_source-member-evidence-review.md` 的 HUST 原包成员审计。
  它与 E1 同属 HUST 来源族，不是独立见证。
- `E3` 是 `14_material-visual-observation.md` 的已有图像观察，只记录
  可见痕迹。

### 命题处置

- `C1 对象身份`：`blocked`。当前只有数据集路线，没有甲骨、残片、版本、
  图版、馆藏或稳定著录身份。
- `C2 直接字形观察`：E1—E3 为 `direct_checked`，仅限可见痕迹、尺寸和
  来源绑定。
- `C3 同字、异体、近形或构件`：`route_only`。没有第二形体、独立证据族
  或不依赖标签的比较。
- `C4 卜辞字例与上下文`：`blocked`。没有图版、行位、精确字位、邻字、
  OCR 或全文路线。
- `C5 读音或隶定候选`：因 C1、C4 和释读史、替代读法缺失而 `withheld`。
- `C6 语义或语法功能`：`withheld`。没有句子上下文或独立字形身份。
- `C7 历时字形对应`：`withheld`。没有带时期来源的桥梁证据。
- `C8 完整命题与用户交付`：`withheld`。权利、上下文、独立证据、校准及
  C1—C7 阻断项均未解决。

### AI 决定

当前决定为 `abstain_withhold_candidate`。不交付读音、意义、概率或面向
用户的候选。本对象仍为 `unknown_candidate_id_not_formal_obs_char_assignment`。

### 证伪条件

- 若获准图版把图像对应到其他对象，或证明文件名只是占位符，撤回身份路线。
- 若第二视角证明痕迹来自残损、裁切、摹写或邻字，撤回任何字形归类主张。
- 只有在独立记录原始对象路线、第二证据族和有日期的释读史后，才可重开。

本页不产生释读、意义、概率或破译结论。
