# Current Operating Baseline / 当前运行基线

## Status / 状态

- Status / 状态: `current_operating_baseline`
- Effective date / 生效日期: `2026-08-30`
- Bound commit / 绑定提交:
  `d1d1653ce90fb9949807dedf50ee577d13747a22`
- Supersedes / 替代:
  `03_current-operating-baseline-2026-08-30.md`
- Research state / 研究状态: `preformal_evidence_opening`
- Probability channel / 概率通道: `withheld`

This is a replaceable operating baseline, not a second normative strategy,
a completion declaration, or a decipherment result. The normative document
remains the [autonomous candidate adjudication strategy][strategy].

这是一份可替换的运行基线，不是第二份规范战略、完成声明或释读结果。
规范文件仍是[自主候选裁决战略][strategy]。

## Current Decision / 当前决定

The unit of progress is one human-readable proposition adjudication with
opened evidence, counterevidence, falsifiers, and a reproducible source route.
A file count, directory count, field list, or generated template is not a
success unit.

进展单位是一项以人类可读方式交付的命题裁决。它必须包含已打开证据、
反证、可推翻条件和可复跑来源路线。文件数量、目录数量、字段清单或生成
模板不是成功单位。

AI agents may make a bounded AI decision for C1--C4: support a limited
candidate, `withhold`, or `abstain`. Human-specialist approval is not a
prerequisite for that decision. C5--C6 reading or semantic candidates still
require opened context, reading history, competing explanations, and
counterevidence.

AI agents 可自主对 C1--C4 作有限裁决：支持有限候选、`withhold` 或
`abstain`。该裁决不以真人专家批准为前提。C5--C6 释读或语义候选仍须
先打开上下文、释读史、竞争解释和反证。

A calibrated probability and `ai_adjudicated_candidate` delivery remain
closed until every C8 calibration, leakage, independent-rerun, rights, and
external-scoring condition is satisfied. Agent confidence is not a research
probability.

校准概率和 `ai_adjudicated_candidate` 交付仍处于关闭状态。只有 C8 的
校准、泄漏、独立复跑、权利和外部评分条件全部满足后才能开放。Agent
自报置信度不是研究概率。

## Gate Snapshot / 门槛快照

- Gate 0: `PASS`. The normative strategy and human-first boundary exist.
- Gate 1: `PARTIAL`. Audited routes are traceable, but source rights and
  derivative ancestry still block several evidence families.
- Gate 2: `BLOCKED`. No eligible family-isolated calibration cohort exists.
- Gate 3: `BLOCKED`. The diagnostic ended `diagnostic_fail_withheld`; there
  is no real v2 laboratory run or external score receipt.
- Gate 4: `ACTIVE_NOT_EXITED`. Deep evidence pilots exist, but stable
  dependency-labelled reruns have not been demonstrated.
- Gate 5: `CLOSED`. No live unknown-character delivery is authorized.
- Gate 6: `CLOSED`. Expansion cannot precede calibration and stable reruns.

- Gate 0：`PASS`。规范战略和人类档案优先边界已经建立。
- Gate 1：`PARTIAL`。已审计路线可追溯，但部分来源仍受权利和派生祖先
  缺口阻断。
- Gate 2：`BLOCKED`。尚无合格的来源家族隔离校准群。
- Gate 3：`BLOCKED`。诊断为 `diagnostic_fail_withheld`，尚无真实 v2
  实验运行或外部评分回执。
- Gate 4：`ACTIVE_NOT_EXITED`。已有深度证据试点，但尚未证明稳定的依赖
  标注复跑。
- Gate 5：`CLOSED`。不授权真实未释字候选交付。
- Gate 6：`CLOSED`。校准和稳定复跑完成前不得扩张。

## Three Active Decisions / 三项当前裁决

### HYZ421

The 2003 source-edition pages `840`, `841`, `1452`, and `1724` are directly
checked. They establish a C4 textual baseline but do not establish that the
inscription means rain avoidance. The competing sacrifice or ritual-food
reading remains blocked until Han Jiangsu pages `350--354` are opened.

2003 年底本第 `840`、`841`、`1452`、`1724` 页已直接复核，形成 C4 文本
基线，但不能证明卜辞含义是避雨。“祭祀或祭食”的竞争解释仍在韩江苏
第 `350--354` 页打开前阻断。

Next decisive action: obtain and inspect only those five Han Jiangsu pages,
then compare their exact argument and citations with the 2003 baseline.

下一决定性动作：只取得并复核韩江苏这五页，再把准确论证和引文与
2003 年底本逐项比较。

### H2 and Heji 2 / H2 与《合集》2

National Library item `2022JGTP0627` binds `北圖14427`, `善9025`, and
`合集2` to an opened rubbing and source-reported partial text. The physical
object route `甲骨14427T` is currently broken. C1 remains a strong object-
identity candidate, not a confirmed five-identifier equivalence.

国家图书馆条目 `2022JGTP0627` 把 `北圖14427`、`善9025` 和 `合集2`
连接到已打开拓片及来源报告残辞。实物条目 `甲骨14427T` 当前失效。
C1 仍是强对象身份候选，不是五种编号已经确认等同。

Next decisive action: recover the physical-object catalog export and a
written source that explicitly relates all identifiers. C5--C6 remain
blocked and C8 remains `withheld`.

下一决定性动作：取得实物条目导出及明确连接全部编号的书面来源。C5--C6
继续阻断，C8 继续 `withheld`。

### `obs-unk-005708`

All 50 HUST source members were visually opened across 13 source-number
groups. The evidence supports C2 visual observations and a dispute about
dataset grouping. Strong mixed-group counterevidence blocks promotion to
same-sign, variant, reading, or meaning.

HUST 的 50 个来源成员已全部打开，覆盖 13 个来源号组。现有证据只支持
C2 视觉观察和“数据集分组有争议”。强混组反证阻止提升为同字、异体、
释读或意义候选。

The official Yinqi Wenyuan search records `合14496` as
`SYS_FLD_SYSID=124308` and `甲3472` as `SYS_FLD_SYSID=263185`. Their
150-pixel thumbnails are visually compatible but not pixel-identity evidence;
the two records are one platform family. The detail endpoint remains
login-gated with HTTP 406.

殷契文渊官方检索记录 `合14496`（`SYS_FLD_SYSID=124308`）和 `甲3472`
（`SYS_FLD_SYSID=263185`）。两张 150 像素缩略图视觉相容，但不是像素
同一证据；两条记录属于同一平台家族。详情接口仍以 HTTP 406 要求登录。

The IHP institutional archive now supplies a stable detail receipt for
`fsnrb188477-3472`: `《小屯第二本殷虛文字甲編》` plate `3472`, Heji
`014496`, period `商後期，第一期`, topic `17.鬼神崇拜`, original rubbing
size `4.29 x 8.54 cm`, and five source-media identifiers. The public image
opened here is only `90 x 63`; no exact `U61812` sign position is recorded.

史语所机构档案现已提供 `fsnrb188477-3472` 的稳定详情回执：
`《小屯第二本殷虛文字甲編》` 图版 `3472`、合集 `014496`、分期
`商後期，第一期`、主题 `17.鬼神崇拜`、原拓尺寸 `4.29 x 8.54 cm`，
以及五个原始媒介编号。本次打开的公开图像只有 `90 x 63`，尚未记录
`U61812` 的确切字位。

The IHP route is institutionally independent from HUST-OBC and Zenodo, but
it is not an independent glyph-identity vote. The candidate bridge reached
`U61812` first, and the IHP and Yinqi records share bibliographic labels.
Rights remain unresolved; the receipt is metadata-only.

史语所路线在机构上独立于 HUST-OBC 和 Zenodo，但不是独立的字形同一票。
候选桥先到达 `U61812`，史语所与殷契文渊记录还共享著录标记。权利仍待
确认；本回执只作 metadata。

Current C4 state is `candidate_catalog_route_with_institutional_anchor`.
C1 remains `blocked_candidate_route`; C3 remains `withhold`; C5--C6 remain
`abstain`; C8 remains `withhold`.

当前 C4 状态为 `candidate_catalog_route_with_institutional_anchor`。
C1 继续 `blocked_candidate_route`；C3 继续 `withhold`；C5--C6 继续
`abstain`；C8 继续 `withhold`。

Next decisive action: request a rights-permitted IHP full image or authorized
crop using the five media identifiers. Bind `U61812` to one exact plate
position, preserve neighboring signs and fractures, compare that position
against all 50 HUST members, and resolve whether `合14496` and `甲3472`
refer to one physical object and edition route.

下一决定性动作：用五个媒介编号申请权利允许的史语所完整图像或授权裁切。
把 `U61812` 对齐到一个确切图版字位，保留邻字和裂隙，同 HUST 50 个成员
比较，并查明 `合14496` 与 `甲3472` 是否指向同一实物和版本路线。

The object-local receipt is the file
`20_ihp-independent-catalog-and-plate-receipt.md` in the
`obs-unk-005708` directory. No new image inventory is authorized while this
exact-position check is open.

对象内回执是 `obs-unk-005708` 目录中的
`20_ihp-independent-catalog-and-plate-receipt.md`。在确切字位检查完成前，
不授权再生成图片清单。

## Stop Conditions / 停止条件

- Do not mass-generate templates, dossiers, or evidence packs.
- Do not release an uncalibrated percentage as decipherment probability.
- Do not add votes from sources with shared image or transcription ancestry.
- Do not infer reuse permission merely because a page is public.
- Keep `withhold` when a hard counterexample or decisive source gap remains.

- 不得批量生成模板、档案或 evidence pack。
- 不得把未校准百分比发布为破译概率。
- 不得累加共享图像或释文祖先的来源票数。
- 不得因为网页公开就推定资料可以再利用。
- 存在硬反例或决定性来源缺口时，必须维持 `withhold`。

## Replacement Rule / 替换规则

Create a new dated operating baseline when new material evidence, a rights
change, an identity conflict, or a verified commit changes a gate or active
decision. Preserve this file as a historical receipt when it is superseded;
do not append an unbounded progress log.

当新实物证据、权利变化、身份冲突或已核验提交改变任一门槛或当前裁决时，
新建带日期的运行基线。被替代后保留本文件作为历史回执，不追加无限进度日志。

[strategy]: README.md
